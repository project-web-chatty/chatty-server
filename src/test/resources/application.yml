#spring:
 # datasource:
  #  url: jdbc:h2:mem:testdb;MODE=MySQL
   # driver-class-name: org.h2.Driver
   # username: sa
   # password: password
 # jpa:
  #  hibernate:
   #   ddl-auto: create
   # show-sql: true
   # properties:
    #  hibernate:
    #    dialect: org.hibernate.dialect.H2Dialect
spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: ${LOCAL_MYSQL_URL}
    username: ${LOCAL_MYSQL_USERNAME}
    password: ${LOCAL_MYSQL_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate # according to our dev environment